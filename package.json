{
  "name": "me-monorepo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "npm run build:site && npm run build:blog",
    "build:site": "npm --prefix site install && npm --prefix site run build",
    "build:blog": "npm --prefix blog install && npm --prefix blog run build",
    "dev:site": "npm --prefix site run dev",
    "dev:blog": "npm --prefix blog run dev"
  }
}
